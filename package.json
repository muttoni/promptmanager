{
  "name": "@rekshaw/promptmanager",
  "version": "0.1.2",
  "description": "Regression-safe prompt + tool-calling evaluator",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "promptmgr": "dist/cli.js"
  },
  "files": [
    "dist",
    "runtime",
    "templates",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "commander": "^14.0.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
